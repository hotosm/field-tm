# Deployment Config

- Docker compose based deployments.
- In production, HOT uses the Kubernetes Helm charts.

## Compose Files

- `compose.odk.yaml`: deploy ODK Central.
- `compose.qfield.yaml`: deploy QFieldCloud.
- `compose.sub.yaml`: deploy FieldTM for your chosen domain.
